import {createContext} from 'react';

//funcion que retorna un estado
const MoviesContext = createContext(); //El ponerlo en variable sirve en el caso que tengamos varios estados separados

export default MoviesContext;