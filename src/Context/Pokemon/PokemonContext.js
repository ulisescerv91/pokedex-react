import {createContext} from 'react';

//funcion que retorna un estado
const PokemonContext = createContext(); //El ponerlo en variable sirve en el caso que tengamos varios estados separados

export default PokemonContext;